[[1. Gestión del Sistema y Entorno (`sys` y `os`)]] [[2. Manipulación Avanzada de Rutas (`pathlib`)]] [[3. Matemáticas y Números Aleatorios (`math` y `random`)]] [[4. Medición de Tiempo y Fechas (`time` y `datetime`)]] [[5. Serialización y Estructuras de Datos (`json` y `csv`)]] [[6. Herramientas Avanzadas de Iteración y Funciones (`itertools` y `functools`)]] [[7. Criptografía (´secrets´)]] 