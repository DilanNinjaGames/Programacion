
```Python
  
 
# ── secrets: para criptografía ────────────────────────────────────────────

import secrets

token = secrets.token_hex(32)        # 64 chars hexadecimales, impredecible

pin   = secrets.randbelow(10**6)     # entero seguro para OTPs

print(f"Token: {token}")

```

  

---
