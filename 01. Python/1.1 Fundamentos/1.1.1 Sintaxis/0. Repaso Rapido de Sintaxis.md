[[1. Fundamentos del lenguaje y Estructuras de Control]] [[2. Funciones Integradas (Built-in Functions) y Funciones Anónimas]] [[3. Cadenas de Texto]] [[4. Listas]] [[5. Tuplas]] [[6. Diccionarios]] [[7.1 `yield` vs `return`]] [[7.2 Generator Expressions vs List Comprehensions]] [[7.3 Módulo `itertools` para Optimización]] [[8. POO — Orientación a Objetos]]  [[3.3 El Estándar de Python- `.sort()` y `sorted()` — Timsort]] [[0. Repaso rápido Estructuras de Datos]] [[0. Complejidad Big 0 [[0. Repaso Rápido Librerías]]]] 
# TIPOS Y CONTROL
type(x) | isinstance(x, int) | x if cond else y
while chunk := f.read(8192): ...          # Walrus operator

# BUILT-INS NUMÉRICOS
bin(10)='0b1010' | hex(255)='0xff' | oct(8)='0o10'
divmod(17, 5) → (3, 2) | pow(2, 10, 1000) → 24 (mod)

# BUILT-INS DE ITERACIÓN
enumerate(lst, start=1) | zip(a, b) | sorted(x, key=fn, reverse=True)

# LAMBDA + FUNCIONALES
sorted(lst, key=lambda x: x[1])
list(filter(lambda x: x > 0, lst))
list(map(lambda x: x**2, lst))

# STRINGS
f"{valor:.2f}" | f"{texto:>10}" | f"{num:0>5}" | s.split(',') | ','.join(lst)

# LISTAS
lst[::2] | lst[::-1] | lst.copy() | [x**2 for x in lst if x > 0]

# DICCIONARIOS
d.get(k, default) | d.setdefault(k, []).append(v)
{k: v for k, v in d.items() if v > 0}

# GENERADORES
(x**2 for x in range(1000))           # Sin cargar en memoria
yield valor                             # Pausa y devuelve

# POO
class Hijo(Padre): super().__init__()
__str__ | __repr__ | __len__ | __eq__
@property | @classmethod | @staticmethod