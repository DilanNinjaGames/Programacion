
```Java
List<Integer> listaID = tasks.stream().map(task -> taskId).toList();
```
Este es el mayor equivalente a las list conprehencion de Python, lo que hace es agarrar una lista y de esa lista mapea las variables solicitadas para despues transformalas a una lista, igual se los puede filtrar con otro metodo